<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1, shrink-to-fit=no"
    />

    <link
      href="https://fonts.googleapis.com/css2?family=Poppins:wght@100;200;300;400;500;600;700;800;900&display=swap"
      rel="stylesheet"
    />

    <title>Hubs</title>
    <script src="https://cdn.socket.io/socket.io-3.0.1.min.js"></script>
    <link rel="stylesheet" href="styles\chat.css" />
    <!-- Bootstrap core CSS -->
    <link href="vendor/bootstrap/css/bootstrap.min.css" rel="stylesheet" />
    <link rel="icon" type="image/png" href="images/favicon.png" />

    <!-- Additional CSS Files -->
    <link rel="stylesheet" href="assets/css/fontawesome.css" />
    <link rel="stylesheet" href="assets/css/templatemo-cyborg-gaming.css" />
    <link rel="stylesheet" href="assets/css/owl.css" />
    <link rel="stylesheet" href="assets/css/animate.css" />
    <link
      rel="stylesheet"
      href="https://unpkg.com/swiper@7/swiper-bundle.min.css"
    />
    <!--
    
    TemplateMo 579 Cyborg Gaming
    
    https://templatemo.com/tm-579-cyborg-gaming
    
    -->

    <link rel="stylesheet" href="styles\hubs/style.css" />
  </head>

  <body>
    <%- include("partials/header"); -%>

    <div class="container my-2">
      <div class="row">
        <div class="col-lg-12">
          <div class="page-content">
            <!-- ***** Details Start ***** -->
            <div class="game-details">
              <div class="row">
                <% if ( locals.login) { %>
                <div class="col-lg-12">
                  <h2 id="form_header" class="mb-5">LogIN</h2>
                </div>
                <div
                  class="col-lg-12 d-flex flex-column align-items-center mb-4"
                >
                  <form method="post" style="width: 350px" action="/login">
                    <div class="form-floating">
                      <input
                        autocomplete="off"
                        type="text"
                        name="username"
                        class="form-control top"
                        placeholder="Username"
                        required
                        autofocus
                      />
                      <label>User Name</label>
                    </div>
                    <div class="form-floating">
                      <input
                        autocomplete="off"
                        type="password"
                        name="password"
                        class="form-control bot"
                        placeholder="password"
                        required
                        autofocus
                      />
                      <label>password</label>
                    </div>

                    <button
                      class="w-100 btn mt-3 btn-lg btn-danger"
                      type="submit"
                    >
                      Login
                    </button>
                    <p class="mt-4 mb-1 text-body-secondary text-center">
                      dont have an account <a href="/signup">signup</a>
                    </p>
                    <% if (locals.msg) {%>
                    <h5 class="text-danger text-center"><%= msg %></h5>
                    <% }%>
                  </form>
                </div>
                <% } else { %>
                <div class="col-lg-12">
                  <h2 id="form_header" class="mb-5">SignUp</h2>
                </div>
                <div
                  class="col-lg-12 d-flex flex-column align-items-center mb-4"
                >
                  <form method="post" style="width: 350px" action="/signup">
                    <div class="form-floating">
                      <input
                        autocomplete="off"
                        type="text"
                        name="name"
                        class="form-control top"
                        placeholder="name"
                        required
                        autofocus
                      />
                      <label>Name</label>
                    </div>
                    <div class="form-floating">
                      <input
                        autocomplete="off"
                        type="text"
                        name="username"
                        class="form-control mid"
                        placeholder="Username"
                        required
                        autofocus
                      />
                      <label>User Name</label>
                    </div>

                    <div class="form-floating">
                      <input
                        autocomplete="off"
                        type="text"
                        name="password"
                        class="form-control bot"
                        placeholder="password"
                        required
                        autofocus
                      />
                      <label>password</label>
                    </div>

                    <button
                      class="w-100 btn mt-3 btn-lg btn-danger"
                      type="submit"
                    >
                      SignUp
                    </button>
                    <p class="mt-4 mb-1 text-body-secondary text-center">
                      have an account <a href="/login">Login</a>
                    </p>
                    <% if (locals.msg) {%>
                    <h5 class="text-danger text-center"><%= msg %></h5>
                    <% }%>
                  </form>
                </div>
                <% } %>
              </div>
              <!-- ***** Details End ***** -->
            </div>
          </div>
        </div>
      </div>
    </div>
    <%- include("partials/footer"); -%>
    <script src="scripts\hubs/index.js"></script>
  </body>
</html>
